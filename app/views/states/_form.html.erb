<%= simple_form_for(@state, :html => {:class => 'form-vertical'}) do |f| %>
  
  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.input :abbreviated %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

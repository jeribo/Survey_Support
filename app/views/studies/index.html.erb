<h1>Listing surveys</h1>

<table>
  <tr>
  	<th>id</th>
    <th>Description</th>
    <th></th>
  </tr>

<% @surveys.each do |survey| %>
  <tr>
    <td><%= link_to survey.id, survey_path(survey) %></td>
    <td><%= survey.description %></td>
    <td><%= link_to 'Set', examinations_study_path(survey) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey', new_survey_path %>